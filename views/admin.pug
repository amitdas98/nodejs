extends layout/main-layout 
block content
    main
        form.userform(action='/admin/add-user', method='POST')
            .form-control
                label(for='username') Please Enter User :
                input#username(type='text', name='username')
            button.button(type='submit') Add user 